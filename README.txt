
Python preparations:
=================================

(Python3 installation assumed)
install pip: sudo apt install python3-pip 

Python libraries:
---------------------------------
pip3 install numpy
pip3 install mysql-connector-python
pip3 install jep

Windows: use `py -m pip` for `pip3` 

